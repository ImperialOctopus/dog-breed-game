import 'world.dart';

/// Data to build the learn section.
class LearnStructure {
  /// List of worlds.
  final List<World> worlds;

  /// Data to build the learn section.
  const LearnStructure({required this.worlds});
}
